import { FilterDinosPipe } from './filter-dinos.pipe';

describe('FilterDinosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDinosPipe();
    expect(pipe).toBeTruthy();
  });
});
